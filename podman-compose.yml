spring:
  datasource:
    url: jdbc:mysql//${DATABASE_HOST:localhost}:${DATABASE_PORT:3306}/${DATABASE_NAME:chusit_db}
    username: ${DATABASE_USER:root}
    password: ${DATABASE_PASSWORD:Ashok311}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  flyway:
    enabled: true
    locations:
      -classpath:bd/migration
    messages:
      basename: i18n/messages

